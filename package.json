{
  "name": "jp-rolloff",
  "version": "1.0.0",
  "private": true,
  "description": "JP Roll-Off Service — dumpster rental site (Eleventy static build)",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "markdown-it": "^14.2.0"
  }
}
